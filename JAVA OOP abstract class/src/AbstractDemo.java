
public abstract class AbstractDemo {

	
	//in c++ pure virtual function is called abstract function
	//and declared as void findArea()=0;
	//and class that contains pure virtual function is by default becomes and called abstract class
	
	//but in java we have keyword "abstract" to make methods and classes abstract.
	//abstract class can be declared without any abstract method
	
}
